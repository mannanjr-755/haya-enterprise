# Database — local dev (run `npm run dev:db` first, then use this URL):
DATABASE_URL="postgres://postgres:postgres@localhost:51214/haya_erp?sslmode=disable&pgbouncer=true"

# Production (Vercel): set your hosted PostgreSQL URL in Vercel env vars, then run `npm run db:setup`
# DATABASE_URL="postgresql://user:pass@host:5432/haya_erp?sslmode=require"

# Authentication
DEMO_USERNAME=adminhayaenterprise
DEMO_PASSWORD=hayaenterprise123
JWT_SECRET=haya-enterprise-jwt-secret-change-in-production
